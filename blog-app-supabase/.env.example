# Copy to .env and set values. Do not commit .env.
# For Dokploy: set these in the project environment / secrets.

# ---------------------------------------------------------------------------
# Local Supabase container (docker-compose)
# ---------------------------------------------------------------------------
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me
POSTGRES_DB=postgres
POSTGRES_PORT=5432

# App: use host "supabase-db" when running in Docker (not localhost).
# Optional: if unset, docker-compose builds DATABASE_URL from POSTGRES_* with host "supabase-db".
DATABASE_URL=postgresql://postgres:change-me@supabase-db:5432/postgres
APP_PORT=3000

# ---------------------------------------------------------------------------
# Supabase cloud (alternative: use cloud DB instead of local container)
# ---------------------------------------------------------------------------
# Get from: Supabase Dashboard → Project Settings → Database → Connection string (URI)
# DATABASE_URL="postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres?pgbouncer=true"

# Optional: Supabase Auth, Realtime, Storage (client-side)
# NEXT_PUBLIC_SUPABASE_URL="https://[PROJECT-REF].supabase.co"
# NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"
